# Session s006 — p002: cycle-3 rerun after bug fixes

- **Date:** 2026-09-08 (UTC)
- **Goal (user):** option 1 — fix the two noted bugs, then re-run cycle 3 of marathon `20260908-0233`.

## Context from s005
- Cycle 2 shipped (PR #8, +3356/−288, 5/5 engineers, QA/review/design ok).
- Cycle 3 assembler timed out (1500s watchdog) seconds before finishing PRD.md; marathon parked
  (`marathon-stopped-waiting-product`). `cmd_report` crashed on worker rows with `start=None`.

## Changes (`p002-selfdev-engine/scripts/driver.py`)
1. `cmd_report` (driver.py:~875): `r.get('start','')[-5:]` → `(r.get('start') or '')[-5:]` (and same for
   `end`) — no more NoneType crash on retained failed worker rows.
2. Assembler watchdog 1500 → **3000s**, configurable via `assembler.timeout_secs` (default added).
3. Assembler prompt: added "work time-efficiently … don't re-read every file — cite paths" hint.
4. Discarded the stale uncommitted cycle-3 `PRD.md` in the worktree before re-run.

## Action
- Relaunched `marathon --run 20260908-0233 --start 3 --max 4 --min-gap 0 --repo mark/cloud-pos-system`
  (pid 1948563). Cycle-3 assembler re-running from scratch under the bigger watchdog — log streaming
  (477KB, mtime 14:24 UTC).

## Evidence
- `ENGINE_STATE/runs/20260908-0233/{trail.md,agent-assembler-3.log}` · `/tmp/opencode/marathon-c3.out`

## Follow-ups
- Pending from s005: task-notes knowledge bridge + `--continue` session resume (user approved "good to go",
  not yet implemented).

## s006 addendum — knowledge bridge implemented (2026-09-08)
- `agent_cmd`/`_agent_cmd`/`spawn_agent` gained `session=` → `--session <id>` (opencode resume).
- `_last_session_id(logfile)` tails the run log for the last sessionID (unit-tested).
- Engineer loop: on a re-attempt of a task, resume ITS OWN previous opencode session; engineer prompt now
  points at `ENGINE_STATE/RESEARCH/<run>.md` + `ENGINE_STATE/TASK-NOTES/t*.md` and requires writing
  `TASK-NOTES/t<N>.md` in the same commit; a stub notes file is pre-seeded so it's always committed.
- Applies from cycle 4 onward (cycle 3 already loaded old code). Cycle-3 progress: engineer t1 done (15:06Z).
